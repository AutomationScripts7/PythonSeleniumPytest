#Tuple() - Immutable – cannot be changed after creation (e.g., strings, tuples, integers) (Update/Remove not possible)

#Tuple do all actions like list datatype but not able to perform update and delete operations

val = (1, "python")
print(val)

val[0] = "hello"   ##Doesn't support update
print(val)





